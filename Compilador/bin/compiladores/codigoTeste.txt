// Online Rust compiler to run Rust program online
// Print "Try programiz.pro" message

fn main() {
    let x = 10;
    let y = 20;
    if x < y && y != 0 || x == 10 {
        println!("Expressão lógica verdadeira");
    }
}